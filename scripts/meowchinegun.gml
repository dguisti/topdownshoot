instance_create(x, y, gun);

/*
if gamepad_button_check_pressed(0,gp_shoulderrb)
{
    globalvar mouseh;
    globalvar mousev;
    mouseh = mouse_x;
    mousev = mouse_y;
    if (stamina > 0)
    instance_create(px, py, gun);
}

